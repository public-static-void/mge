{
  "title": "Item",
  "type": "object",
  "description": "Metadata for items including slot compatibility and requirements.",
  "properties": {
    "id": { "type": "string" },
    "name": { "type": "string" },
    "slot": {
      "type": "string",
      "description": "Valid equipment slot for this item"
    },
    "two_handed": { "type": "boolean", "default": false },
    "material": {
      "type": ["string", "null"],
      "description": "Reference to a material definition by name"
    },
    "requirements": {
      "type": ["object", "null"],
      "description": "Minimum stat values required to equip this item.",
      "properties": {
        "strength": { "type": "number" }
      },
      "additionalProperties": { "type": "number" }
    },
    "effects": {
      "type": ["object", "null"],
      "description": "Stat bonuses granted when item is equipped. Any stat name accepted.",
      "properties": {
        "strength": { "type": ["number", "null"] },
        "dexterity": { "type": ["number", "null"] },
        "intelligence": { "type": ["number", "null"] }
      },
      "additionalProperties": { "type": ["number", "null"] }
    }
  },
  "required": ["id", "name", "slot"],
  "modes": ["colony", "roguelike"]
}
