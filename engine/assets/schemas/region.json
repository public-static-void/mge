{
  "title": "Region",
  "type": "object",
  "description": "Assigns an entity to one or more regions/zones (e.g., room, biome, area).",
  "properties": {
    "id": {
      "oneOf": [
        { "type": "string" },
        { "type": "array", "items": { "type": "string" } }
      ],
      "description": "One or more region/zone identifiers"
    },
    "label": { "type": "string", "description": "Human-readable name" },
    "kind": {
      "type": "string",
      "description": "Region type (e.g., 'room', 'biome', 'zone')"
    }
  },
  "required": ["id"],
  "modes": ["colony", "roguelike", "editor"]
}
