[package]
name = "engine_wasm"
version = "0.1.0"
edition = "2024"

[dependencies]
engine_core = { path = "../engine/core" }
wasmtime = { version = ">=36.0.3", features = ["async"] }
anyhow = "1.0"
serde_json = "1.0"

[dev-dependencies]
wat = "1.233"
tempfile = "3.20"
